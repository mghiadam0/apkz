const box=document.querySelector('#calendar'),pick=document.querySelector('#month');async function draw(){let [y,m]=pick.value.split('-').map(Number),first=new Date(y,m-1,1),start=new Date(y,m-1,1-first.getDay()),events=await(await fetch('/api/calendar?month='+pick.value)).json();box.innerHTML='';for(let i=0;i<42;i++){let d=new Date(start);d.setDate(start.getDate()+i);let iso=[d.getFullYear(),String(d.getMonth()+1).padStart(2,'0'),String(d.getDate()).padStart(2,'0')].join('-'),el=document.createElement('div');el.className='day '+(d.getMonth()!==m-1?'muted':'');el.innerHTML='<b>'+d.getDate()+'</b>';events.filter(e=>e.at.slice(0,10)===iso).forEach(e=>{let a=document.createElement('a');a.className='event';a.href='/notes/?q='+encodeURIComponent(e.title);a.textContent=e.title;el.append(a)});box.append(el)}}pick.onchange=()=>{history.replaceState(null,'','?month='+pick.value);draw()};draw();